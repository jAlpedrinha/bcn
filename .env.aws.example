# AWS Configuration Example
# Copy this file to .env.local and fill in your AWS credentials

# S3 Configuration for AWS
# Leave S3_ENDPOINT empty for AWS S3 (or remove the line)
S3_ENDPOINT=
S3_ACCESS_KEY=YOUR_AWS_ACCESS_KEY_ID
S3_SECRET_KEY=YOUR_AWS_SECRET_ACCESS_KEY
S3_REGION=us-east-1

# Path Style Access - AWS S3 uses virtual-hosted style
S3_PATH_STYLE_ACCESS=false

# S3 Buckets
BACKUP_BUCKET=your-backup-bucket-name
WAREHOUSE_BUCKET=your-warehouse-bucket-name

# Catalog Configuration for AWS Glue
CATALOG_TYPE=glue
CATALOG_NAME=glue_catalog

# Hive Metastore URI (not used with Glue)
# HIVE_METASTORE_URI=

# Working directory for temporary files
WORK_DIR=/tmp/bcn

# Notes:
# 1. For AWS Glue, the catalog automatically uses your AWS credentials from:
#    - Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
#    - IAM roles (if running on EC2/ECS/Lambda)
#    - AWS credentials file (~/.aws/credentials)
#
# 2. Make sure your AWS credentials have permissions for:
#    - S3: s3:GetObject, s3:PutObject, s3:ListBucket
#    - Glue: glue:GetDatabase, glue:GetTable, glue:CreateTable, etc.
#
# 3. The buckets must already exist in your AWS account
